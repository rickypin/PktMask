Activate the Python virtual environment (.venv) before executing CLI commands when needed
use python3 but not python when executing CLI commands
Always write code,comments and documents in English
Do NOT create document unless I explicitly request it, and save all design and implementation docs in docs/dev